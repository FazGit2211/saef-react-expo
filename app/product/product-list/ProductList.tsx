import { View } from "react-native"
import { Text } from "react-native-paper";

const ProductList = () => {
    return (
        <View>
            <Text>Listado</Text>
        </View>
    );
};
export default ProductList;