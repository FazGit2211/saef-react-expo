import { StyleSheet } from "react-native"
import CardPlayerNew from "../../components/cards/CardPlayerNew";

const PlayerInfo = () => {
    return (
        <>
            <CardPlayerNew />
        </>
    );
};
const styles = StyleSheet.create({
    container: {
        flex: 1,
    }
})
export default PlayerInfo;